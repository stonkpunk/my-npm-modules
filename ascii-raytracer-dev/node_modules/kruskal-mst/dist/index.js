"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.kruskal = void 0;
var disjoint_set_ds_1 = require("disjoint-set-ds");
function kruskal(edges) {
    edges.sort(function (a, b) {
        return a.weight - b.weight;
    });
    var f = [];
    var ds = new disjoint_set_ds_1.DisjointSet();
    edges.forEach(function (edge) {
        ds.makeSet(edge.from);
        ds.makeSet(edge.to);
    });
    edges.forEach(function (edge) {
        if (ds.find(edge.from) !== ds.find(edge.to)) {
            f.push(edge);
            ds.union(ds.find(edge.from), ds.find(edge.to));
        }
    });
    return f;
}
exports.kruskal = kruskal;

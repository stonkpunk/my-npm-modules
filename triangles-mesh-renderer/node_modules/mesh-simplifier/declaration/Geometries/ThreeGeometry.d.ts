import * as THREE from "three";
import Geometry from "./Geometry";
import Vector3 from "../math/Vector3";
import Face3 from "../math/Face3";
declare class ThreeGeometry implements Geometry {
    originalGeometry: THREE.Geometry | THREE.BufferGeometry;
    private _processingGeometry;
    private _isBufferGeometry;
    constructor(geometry: THREE.Geometry | THREE.BufferGeometry);
    prepare(): {
        vertices: Vector3[];
        faces: Face3[];
    };
    update(datas: {
        vertices: Vector3[];
        faces: Face3[];
        unculledVertices: number[];
        unculledFaces: number[];
    }): this;
}
export default ThreeGeometry;

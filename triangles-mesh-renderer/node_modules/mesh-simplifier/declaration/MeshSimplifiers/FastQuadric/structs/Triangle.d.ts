import Vector3 from "../../../math/Vector3";
declare class Triangle {
    originalIndex: number;
    v: [number, number, number];
    err: [number, number, number, number];
    deleted: boolean;
    dirty: boolean;
    n: Vector3;
    constructor(idx: number);
}
export default Triangle;
